{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "assemble",
            "type": "shell",
            "command": "nasm",
            "args": ["-f", "elf64", "${file}", "-o", "${fileDirname}/${fileBasenameNoExtension}.o"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
